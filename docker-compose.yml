version: "3"
services:
  translator:
    build: .
    ports:
      - "3001:3000"
    env_file:
      - .env
    environment:
      - PORT=3000
    restart: always
    volumes:
      - ./public:/app/public
